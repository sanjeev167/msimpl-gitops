# =============================================================================
# Kubernetes Namespace
# =============================================================================
# File        : namespace.yaml
# Project     : MSIMPL - Microservices Platform
# Purpose     : Creates the dedicated namespace for all MSIMPL Kubernetes
#               resources.
# Version     : 1.0.0
# =============================================================================

apiVersion: v1
kind: Namespace

metadata:
  name: msimpl

  labels:
    app.kubernetes.io/name: msimpl
    app.kubernetes.io/part-of: microservices-platform